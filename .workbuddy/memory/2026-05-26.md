# 2026-05-26

## 向前端和数据库分别插入10道真实AIGC实训题目
- 直接操作 SQLite 数据库 `backend/aigc_training.db` 插入10道题目
- 同时在 `src/api/index.js` 中新增 `getDefaultTasks()` 函数，页面加载时自动填充10道默认题目到 localStorage
- 前端完全不依赖后端，题目在 localStorage 初始化时自动写入
- 后端 `backend/app/seed_tasks.py` 也加入了种子数据，Railway重启时自动初始化
- 题目类型分布：text x3, image x3, audio x2, video x2
- 教师ID=2（teacher用户），所有任务的 creator_id/teacher_id 均为2
- 已构建部署到 GitHub Pages (commit: c52680f)

## Railway 后端502
- Railway 后端 `aigc-platform-production.up.railway.app` 当前返回502
- 前端不受影响，因为所有数据走 localStorage 本地存储

## 修复管理员用户管理页面看不到用户
- 根因：`src/api/index.js` 中 `path = url.replace(/^\//, '')` 没有去掉查询字符串，导致 `/api/admin/users?skip=0&limit=20` 无法匹配到处理器，直接抛 "API 未实现"
- 修复：改为 `url.replace(/^\//, '').split('?')[0]`
- 同时给 admin users GET 接口加了 role/search/skip/limit 过滤逻辑
- 修复了 UserManagement.vue 中分页 total 用分页后数据长度计算的 bug
- 已构建部署 (commit: 20cd61c, gh-pages: c2d0634)

## 实现内容审核功能
- 根因：ContentReview.vue 调用的 `/api/admin/contents/pending` 和 `/api/admin/contents/{id}/review` 在 API 模块中完全没有实现
- 新增 `content_reviews` localStorage 存储，每次文本/图像生成时自动插入待审核记录
- 新增 3 个 API 端点：pending（获取列表+状态筛选）、review（通过/拒绝）、stats（统计）
- 一次性迁移：将已有的 text/image/audio/video_history 记录迁入审核表，标记为"已通过"
- ContentReview.vue 重写：顶部统计卡片（全部/待审核/已通过/已拒绝），支持筛选、图片预览、详情查看、撤回
- 系统配置 `require_content_review` 改为 `true`
- admin statistics 的 total_contents 和 pending_reviews 现在返回真实数据
- 已构建部署 (commit: b95d5e1, gh-pages: 143fdd8)

## 美化教师端首页仪表盘
- 参考学生端 Dashboard.vue 的玻璃拟态、渐变、浮动卡片等视觉风格
- 新增 Hero 欢迎区（渐变标题、浮动卡片动画、统计概览）
- 新增快捷操作区：实训任务、作品管理、学习资源、导出报表（4 个 glass-card 卡片）
- 核心指标面板改为双列图标卡片样式，嵌入时间筛选器
- 图表区域包裹 glass-card，样式统一
- 最近提交改为侧边卡片列表（头像+信息+状态标签）
- 新增今日提示面板
- 整体采用 CSS 变量体系，与学生端视觉一致
- 保留全部原有功能：图表、导出报表、批改弹窗、数据加载逻辑
- 已构建部署 (gh-pages: a1cde976)

## 修正教师端首页快捷操作跳转链接
- `作品管理` 卡片：从 `/student-works` 改为 `/my-works`
- `学习资源` 卡片：从 `/learning-resources` 改为 `/resource-center`
- `查看全部` 按钮：从 `/student-works` 改为 `/my-works`
- 已构建部署 (gh-pages: ea63ed2b)

## 修复 GitHub Pages 黑屏（dist JS 引用不匹配）
- 根因：main 分支 dist/index.html 引用 `index-RgvASNjk.js`，但该文件不存在于仓库中（Vite content-hash 每次构建不同）
- 修复：`npm run build` 重新生成 dist，新入口为 `index-Ct510C1m.js`
- gh-pages 分支（43c8329e）文件一直正确，无需更新
- main 分支已提交 (commit: 010e1a76)，已推送 origin main
- 线上验证：index.html / JS / CSS 均 200 OK，黑屏问题已解决
