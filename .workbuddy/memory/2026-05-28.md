# 2026-05-28

## 接入魔塔社区 DashScope Wan2.1 视频生成 API
- 修改 `src/api/index.js`：添加 `DASHSCOPE_API_KEY`、`_dashscopeFetch`、`submitVideoTask`、`queryVideoTask` 三个新方法
- 修改 `src/views/VideoGenerate.vue`：完全重写生成逻辑，从假进度改为真实异步提交+轮询
  - 提交任务到 DashScope（POST /video-synthesis），获取 task_id
  - 每 10 秒轮询任务状态（GET /tasks/{task_id}）
  - SUCCEEDED 时获取 video_url 并播放，同时保存到 localStorage
  - FAILED/PENDING/RUNNING 各状态都有对应提示和进度
- 参数面板更新：时长 5/10/15 秒、分辨率 720P/1080P、画面比例 16:9/9:16/1:1
- 画面风格（写实/卡通/科幻/油画）自动映射为 prompt 前缀
- 使用 corsproxy.io 作为 CORS 代理（与智谱 AI 相同模式）
- 通过 DashScope API 调用魔塔社区 Wan2.1 视频生成模型
- 已构建部署 (main: b9b35a6d, gh-pages: 70dfcf5d)
