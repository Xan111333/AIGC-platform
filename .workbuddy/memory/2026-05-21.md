# 2026-05-21

## AIGC项目部署进展

### Railway后端部署 ✅
- 后端已成功部署到 Railway，公网域名：https://aigc-platform-production.up.railway.app
- Root Directory 设置为 ackend
- 环境变量：SECRET_KEY, DATABASE_URL, ZHIPU_API_KEY
- 端口：8000（Railway映射后对外提供服务）
- 本地无Git，通过PowerShell安装Git后成功push

### 前端API地址更新 ✅
- 已将 src/api/index.js、src/views/Register.vue、src/views/teacher/Dashboard.vue 中的 localhost:8000 替换为 Railway 域名
- 已推送到 GitHub，触发 GitHub Pages 自动重新构建

### 部署配置
- 后端：Railway（端口8000，SQLite数据库）
- 前端：GitHub Pages（Vite构建）
- API：智谱AI（GLM-4 + CogView-4）

### 备注
- 本机PATH中没有git，临时安装 Git-2.45.0 解决
- GitHub PAT已保存在 C:\Users\Administrator\.git-credentials
- 强制推送覆盖了远程（远程有其他提交冲突）

## 12:16 - 本地前后端启动成功
- Railway 持续502无法部署，转本地运行
- 后端: uvicorn on :8000, 前端: vite on :5173
- API URL 临时改为 localhost:8000
- CORS 添加 localhost 白名单
- GitHub API 直接提交绕过 git 协议阻断（cloudflared/localtunnel/bore 均不可用）
- Railway 最新 commit: ae3cc37 (完整依赖 + CORS 修复) 仍 502
